# Open Questions - P000A Repo Intake Readonly

Mission: `GC-SF-RE-OS-V1-20260701-001`

1. Which existing app or service should become the first build target after
   OpenSpec, architecture, API contract, and validation gates are complete?
2. Should the master OpenSpec change remain
   `gc-sf-re-os-v1-p000a`, or should future packets use one change per product
   slice?
3. Which external sources should be verified first: OpenSpec, 9Router, The
   Agency, Logto, n8n, video-use, Stagehand, or OpenClaw?
4. Which Android Hermes-agent evidence is acceptable for promoting it from
   companion-only to a verified command/control node?
5. Which model/provider gateway is approved for a one-turn read-only review,
   if any? Current default remains no provider calls.
6. Should ClickUp remain a mirror only for this plan, or stay out of P000A
   entirely until a later explicit mirror packet?

## Default Until Answered

Proceed with local documents, OpenSpec drafts, validation plans, receipts, and
handoff only. Do not install, clone, call providers, deploy, push, read secrets,
or run browser automation.
