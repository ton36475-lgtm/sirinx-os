# Legacy P101 Migration Plan

**Status:** `SUPERSEDED_DO_NOT_EXECUTE`
**Replacement:** `docs/plans/GHOSTCLAW_HERMES_V5_MIGRATION_PHASES.md`

This archived plan preserved the original P1-P11 outline. Its production
deployment command was removed because the V5 process ends at a preview tag
and requires a separate production gate.

The legacy sequence covered workspace bootstrap, state-machine and hash-chain
work, read-only Telegram commands, dispatch, routing, hardening, Telegram UX,
outbox/idempotency, dashboard work, and a production-readiness phase. It must
not be used as an execution packet.
