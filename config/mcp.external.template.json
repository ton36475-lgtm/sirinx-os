{
  "purpose": "Template only. Do not copy into .thclaws/mcp.json until tokens and approval are ready.",
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-github"
      ],
      "env": {
        "GITHUB_TOKEN": "set-in-shell-or-keychain"
      }
    }
  },
  "codexConnectorMap": {
    "supabase": "Use Codex Supabase connector for project-aware database work; approval required for writes.",
    "notion": "Use Codex Notion connector for docs/tasks; approval required for writes.",
    "google-drive": "Use Codex Google Drive connector for Drive/Docs/Sheets/Slides; approval required for writes.",
    "clickup": "Use Codex ClickUp connector for task/workflow surfaces; approval required for writes.",
    "figma": "Use Codex Figma connector for designs; approval required for writes.",
    "canva": "Use Codex Canva connector for designs; approval required for writes.",
    "openai-developers": "Use Codex OpenAI Developers plugin for OpenAI API/App work."
  }
}
