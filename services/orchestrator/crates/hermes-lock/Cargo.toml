[package]
name = "hermes-lock"
version = "0.1.0"
edition = "2021"

[dependencies]
hermes-core = { path = "../hermes-core" }
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[lib]
name = "hermes_lock"
path = "src/lib.rs"
